<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>沈林龍 | Audio Engineer & Musician</title>
    <style>
        /* --- 全局变量与重置 --- */
        :root {
            --bg-color: #121212; /* 深邃黑 */
            --card-bg: #1e1e1e;
            --text-main: #e0e0e0; /* 亮灰 */
            --text-muted: #888888; /* 高级灰 - 次要文字 */
            --accent: #d4d4d4; /* 接近白色的灰，用于高亮 */
            --border-color: #333333;
            --font-main: "Helvetica Neue", Arial, "Hiragino Kaku Gothic ProN", "Hiragino Sans", Meiryo, sans-serif;
            --font-tech: "Courier New", Courier, monospace; /* 代码风格字体 */
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-main);
            font-family: var(--font-main);
            line-height: 1.8;
            overflow-x: hidden;
        }

        a {
            text-decoration: none;
            color: inherit;
            transition: color 0.3s ease;
        }

        ul {
            list-style: none;
        }

        /* --- 导航栏 --- */
        nav {
            position: fixed;
            top: 0;
            width: 100%;
            padding: 20px 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: rgba(18, 18, 18, 0.9);
            backdrop-filter: blur(10px);
            z-index: 1000;
            border-bottom: 1px solid var(--border-color);
        }

        .logo {
            font-weight: 700;
            font-size: 1.2rem;
            letter-spacing: 2px;
        }

        .nav-links {
            display: flex;
            gap: 30px;
        }

        .nav-links a {
            font-size: 0.9rem;
            color: var(--text-muted);
            position: relative;
        }

        .nav-links a::after {
            content: '';
            position: absolute;
            width: 0;
            height: 1px;
            bottom: -5px;
            left: 0;
            background-color: var(--accent);
            transition: width 0.3s ease;
        }

        .nav-links a:hover {
            color: var(--accent);
        }

        .nav-links a:hover::after {
            width: 100%;
        }

        /* --- 布局容器 --- */
        .container {
            max-width: 900px;
            margin: 0 auto;
            padding: 0 20px;
        }

        section {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding: 100px 0;
        }

        /* --- Hero 区域 (首页) --- */
        .hero h1 {
            font-size: clamp(3rem, 8vw, 6rem);
            line-height: 1.1;
            margin-bottom: 20px;
            letter-spacing: -2px;
            opacity: 0;
            transform: translateY(30px);
            animation: fadeUp 1s cubic-bezier(0.2, 1, 0.3, 1) forwards 0.5s;
        }

        .hero-subtitle {
            font-family: var(--font-tech);
            color: var(--text-muted);
            font-size: 1.1rem;
            margin-bottom: 40px;
            display: block;
            opacity: 0;
            animation: fadeUp 1s cubic-bezier(0.2, 1, 0.3, 1) forwards 0.8s;
        }

        .tagline {
            font-style: italic;
            color: var(--text-muted);
            border-left: 2px solid var(--accent);
            padding-left: 20px;
            margin-top: 40px;
            opacity: 0;
            animation: fadeIn 1s ease forwards 1.2s;
        }

        /* --- About 区域 --- */
        .section-title {
            font-size: 0.9rem;
            color: var(--text-muted);
            margin-bottom: 40px;
            text-transform: uppercase;
            letter-spacing: 3px;
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .section-title::after {
            content: '';
            height: 1px;
            flex-grow: 1;
            background-color: var(--border-color);
        }

        .about-content p {
            font-size: 1.5rem;
            font-weight: 300;
            margin-bottom: 30px;
            color: var(--text-main);
        }

        .tech-stack {
            display: flex;
            flex-wrap: wrap;
            gap: 15px;
            margin-top: 20px;
        }

        .tech-tag {
            font-family: var(--font-tech);
            font-size: 0.85rem;
            padding: 8px 16px;
            border: 1px solid var(--border-color);
            color: var(--text-muted);
            transition: all 0.3s ease;
            cursor: default;
        }

        .tech-tag:hover {
            border-color: var(--accent);
            color: var(--accent);
            background: rgba(255,255,255,0.05);
        }

        /* --- Works 区域 (文字卡片) --- */
        .works-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 20px;
        }

        .work-card {
            background: var(--card-bg);
            padding: 40px;
            border-left: 3px solid transparent;
            transition: all 0.4s cubic-bezier(0.2, 1, 0.3, 1);
            position: relative;
            overflow: hidden;
            cursor: pointer;
        }

        .work-card:hover {
            transform: translateX(10px);
            border-left-color: var(--accent);
            background: #252525;
        }

        .work-year {
            font-family: var(--font-tech);
            font-size: 0.8rem;
            color: var(--text-muted);
            margin-bottom: 10px;
            display: block;
        }

        .work-title {
            font-size: 1.8rem;
            margin-bottom: 15px;
        }

        .work-desc {
            color: var(--text-muted);
            font-size: 1rem;
            max-width: 80%;
        }

        .work-arrow {
            position: absolute;
            right: 40px;
            top: 50%;
            transform: translateY(-50%);
            opacity: 0;
            transition: all 0.4s ease;
            color: var(--accent);
            font-size: 1.5rem;
        }

        .work-card:hover .work-arrow {
            opacity: 1;
            right: 30px;
        }

        /* --- Footer / Contact --- */
        .contact-area {
            text-align: left;
        }

        .contact-link {
            display: inline-block;
            font-size: 3rem;
            font-weight: 700;
            color: var(--text-main);
            margin-bottom: 20px;
            position: relative;
        }

        .contact-link:hover {
            color: #fff;
            text-shadow: 0 0 20px rgba(255,255,255,0.3);
        }

        footer {
            padding: 50px 0;
            border-top: 1px solid var(--border-color);
            text-align: center;
            font-size: 0.8rem;
            color: var(--text-muted);
            font-family: var(--font-tech);
        }

        /* --- 动画 --- */
        @keyframes fadeUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        @keyframes fadeIn {
            to { opacity: 1; }
        }

        /* 简单的音频波形动画装饰 */
        .wave-container {
            display: flex;
            align-items: center;
            gap: 3px;
            height: 20px;
            margin-bottom: 20px;
        }
        .bar {
            width: 3px;
            background-color: var(--text-muted);
            animation: wave 1s ease-in-out infinite;
        }
        .bar:nth-child(2) { animation-delay: 0.1s; height: 60%; }
        .bar:nth-child(3) { animation-delay: 0.2s; height: 80%; }
        .bar:nth-child(4) { animation-delay: 0.3s; height: 50%; }
        .bar:nth-child(5) { animation-delay: 0.4s; height: 90%; }
        
        @keyframes wave {
            0%, 100% { height: 20%; opacity: 0.5; }
            50% { height: 100%; opacity: 1; }
        }

        /* 响应式 */
        @media (max-width: 768px) {
            .hero h1 { font-size: 3.5rem; }
            .about-content p { font-size: 1.2rem; }
            .nav-links { display: none; } /* 移动端可根据需要添加汉堡菜单，此处简化处理 */
            .work-card:hover { transform: none; background: #252525; }
        }
    </style>
</head>
<body>

    <nav>
        <div class="logo">SHEN LINLONG</div>
        <div class="nav-links">
            <a href="#about">About</a>
            <a href="#works">Works</a>
            <a href="#contact">Contact</a>
        </div>
    </nav>

    <div class="container">
        
        <section class="hero">
            <div class="wave-container">
                <div class="bar"></div><div class="bar"></div><div class="bar"></div><div class="bar"></div><div class="bar"></div>
            </div>
            <span class="hero-subtitle">Audio Engineer & Independent Musician</span>
            <h1>沈林龍</h1>
            <div class="tagline">
                "make some noise"
            </div>
        </section>

        <section id="about">
            <div class="section-title">PROFILE</div>
            <div class="about-content">
                <p>
                    音響技術とプログラミングの交差点で、独自の音像を構築する。
                    <br><br>
                    <span style="color: var(--accent);">VST Plugins</span> や <span style="color: var(--accent);">Max for Live (M4L)</span> デバイスの開発を通じて、
                    従来の枠に囚われない <span style="color: var(--accent);">Sound Design</span> を追求しています。
                    特に <span style="color: var(--accent);">Electronic Music</span> の領域において、
                    実験的かつ機能的な音作りを得意としています。
                </p>
            </div>
            <div class="tech-stack">
                <span class="tech-tag">Audio Engineering</span>
                <span class="tech-tag">Sound Design</span>
                <span class="tech-tag">VST Development</span>
                <span class="tech-tag">Max for Live</span>
                <span class="tech-tag">Electronic Music</span>
                <span class="tech-tag">C++ / JUCE</span>
            </div>
        </section>

        <section id="works">
            <div class="section-title">SELECTED WORKS</div>
            <div class="works-grid">
                
                <div class="work-card">
                    <span class="work-year">2024 / VST PLUGIN</span>
                    <h3 class="work-title">Granular Texture Synth</h3>
                    <p class="work-desc">
                        C++とJUCEフレームワークを用いて開発されたグラニュラー・シンセサイザー。
                        予測不可能なテクスチャ生成に特化したSound Designツール。
                    </p>
                    <div class="work-arrow">→</div>
                </div>

                <div class="work-card">
                    <span class="work-year">2023 / MAX FOR LIVE</span>
                    <h3 class="work-title">Stochastic Sequencer</h3>
                    <p class="work-desc">
                        Ableton Live用の確率的シーケンサーデバイス。
                        Electronic Music制作における偶然性を制御し、複雑なリズムパターンを生成。
                    </p>
                    <div class="work-arrow">→</div>
                </div>

                <div class="work-card">
                    <span class="work-year">2022 / MUSIC PRODUCTION</span>
                    <h3 class="work-title">EP: "Digital Silence"</h3>
                    <p class="work-desc">
                        フィールドレコーディングとデジタルノイズを融合させた、
                        実験的Electronic作品集。全曲のMix/Masteringを担当。
                    </p>
                    <div class="work-arrow">→</div>
                </div>

            </div>
        </section>

        <section id="contact">
            <div class="section-title">GET IN TOUCH</div>
            <div class="contact-area">
                <p style="color: var(--text-muted); margin-bottom: 10px; font-family: var(--font-tech);">Collaboration / Inquiry</p>
                <a href="mailto:your_email@example.com" class="contact-link">hello@shenlinlong.com</a>
                
                <div class="tech-stack" style="margin-top: 40px;">
                    <a href="#" class="tech-tag">GitHub</a>
                    <a href="#" class="tech-tag">SoundCloud</a>
                    <a href="#" class="tech-tag">X (Twitter)</a>
                    <a href="#" class="tech-tag">Instagram</a>
                </div>
            </div>
        </section>

    </div>

    <footer>
        <p>&copy; 2025 SHEN LINLONG. All Rights Reserved.</p>
    </footer>

    <script>
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                document.querySelector(this.getAttribute('href')).scrollIntoView({
                    behavior: 'smooth'
                });
            });
        });

        // 简单的滚动显现效果 (Intersection Observer)
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = 1;
                    entry.target.style.transform = 'translateY(0)';
                }
            });
        });

        // 对 Work Cards 应用滚动显现效果
        document.querySelectorAll('.work-card').forEach((el) => {
            el.style.opacity = 0;
            el.style.transform = 'translateY(20px)';
            el.style.transition = 'all 0.6s ease-out';
            observer.observe(el);
        });
    </script>
</body>
</html>